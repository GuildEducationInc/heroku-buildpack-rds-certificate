#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

BUILD_DIR=$1
CERT_URL=https://s3.amazonaws.com/rds-downloads/rds-combined-ca-bundle.pem

echo "Downloading ${CERT_URL}"

curl --fail -v ${CERT_URL} > $BUILD_DIR/rds-combined-ca-bundle.pem
exit $?
